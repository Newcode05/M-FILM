import { Login } from "./login";
export default {
    'title': 'Component/Login/component/login',
    'component': Login
}
const Template = (args) => <Login {...args} />
export const Default = Template.bind({});