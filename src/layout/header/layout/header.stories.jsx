import { Header } from "./header";
export default {
    title: 'Header/Layout/Header',
    component: Header
}
const Template = (args) => <Header {...args} />
export const Default = Template.bind({});