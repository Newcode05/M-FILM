import { Register } from "./register";
export default {
    title: 'Login/Component/Register',
    component: Register
}
const Template = (args) => <Form {...args} />
export const Default = Template.bind({});